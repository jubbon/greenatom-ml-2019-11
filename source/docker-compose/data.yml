version: '3'

services:
  data:
    image: citylix-greenatom-ml/data:develop
    build: ../images/data
    environment:
      DATA_DIR: /data/playbooks
    volumes:
      - ../../data/playbooks:/data/playbooks
